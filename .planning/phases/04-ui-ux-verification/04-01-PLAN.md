---
phase: 04-ui-ux-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/OwnerDashboard.tsx
  - src/pages/FrontDesk.tsx
  - src/hooks/useOrdersFeed.ts
files_deleted:
  - src/pages/BakerStation.tsx
  - src/components/kitchen/BakerTicketCard.tsx
  - src/components/dashboard/FrontDeskLayout.tsx
  - src/components/dashboard/DashboardLayout.tsx
  - src/components/dashboard/FrontDeskSidebar.tsx
  - src/components/dashboard/OrderCalendarView.tsx
  - src/components/dev/
autonomous: true

must_haves:
  truths:
    - "npm run build completes without TypeScript errors or warnings"
    - "No console.log or console.error debug statements remain in dashboard production code"
    - "Dead code files no longer exist on the filesystem"
  artifacts:
    - path: ".planning/phases/04-ui-ux-verification/04-01-SUMMARY.md"
      provides: "Summary of code cleanup completed"
      min_lines: 20
  key_links: []
---

<objective>
Delete remaining dead code files from the filesystem and remove debug console statements from production dashboard code.

Purpose: Clean up the codebase so Phase 4 verification starts from a clean baseline — no ghost files, no debug noise in the browser console.
Output: A clean build with no untracked dead code files and no production console logging.
</objective>

<execution_context>
@/Users/luismiguel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luismiguel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-verification/03-01-SUMMARY.md
</context>

<tasks>

<task type="execute" id="04-01-T1">
  <name>Task 1: Delete dead code files from filesystem</name>
  <description>
Plan 03-01 removed these files from git tracking but they still exist on disk as untracked files (shown as ?? in git status). Delete them with rm since they are not imported by any active code.

Files to delete:
- src/pages/BakerStation.tsx
- src/components/kitchen/BakerTicketCard.tsx
- src/components/dashboard/FrontDeskLayout.tsx
- src/components/dashboard/DashboardLayout.tsx
- src/components/dashboard/FrontDeskSidebar.tsx
- src/components/dashboard/OrderCalendarView.tsx
- src/components/dev/ (entire folder)

**Before deleting:** grep each file to confirm it is not imported by any active .tsx or .ts file in src/ (excluding the files being deleted). If any active file imports one of these, STOP and report — do not delete.

**Command pattern:**
```bash
grep -r "BakerStation\|BakerTicketCard\|FrontDeskLayout\|DashboardLayout\|FrontDeskSidebar\|OrderCalendarView\|DevTools" src/ --include="*.tsx" --include="*.ts" -l
```

Only proceed with deletion if no active files import them.
  </description>
  <verification>
    - `git status` shows no ?? entries for deleted files
    - `grep -r "BakerStation" src/` returns nothing in active files
  </verification>
</task>

<task type="execute" id="04-01-T2">
  <name>Task 2: Remove debug console statements from OwnerDashboard.tsx</name>
  <description>
Remove two debug console statements from src/pages/OwnerDashboard.tsx:

1. Line ~138: `console.log('🔄 OwnerDashboard: Fetching fresh data...');`
   - Simply delete this line. The fetch itself still works without the log.

2. Line ~172: `console.error('❌ Error loading dashboard:', error);`
   - Replace with a toast notification using sonner (already imported):
     `toast.error(t('Error al cargar el dashboard', 'Failed to load dashboard'));`
   - This way errors are still visible to the user/developer but not polluting the console.
  </description>
  <verification>
    - `grep "console.log\|console.error" src/pages/OwnerDashboard.tsx` returns nothing
    - Error handling still exists (just via toast now)
  </verification>
</task>

<task type="execute" id="04-01-T3">
  <name>Task 3: Remove debug console statements from FrontDesk.tsx</name>
  <description>
Remove or replace raw console.error() calls in src/pages/FrontDesk.tsx.

For each console.error related to email failures:
- If there's already a toast.error or user-facing feedback nearby, simply delete the console.error line.
- If there's no user-facing error handling, replace `console.error(error)` with `toast.error(t('Error', 'An error occurred'))` to maintain feedback.

Do not remove any try-catch structure — only the raw console statements.
  </description>
  <verification>
    - `grep "console\." src/pages/FrontDesk.tsx` returns nothing
  </verification>
</task>

<task type="execute" id="04-01-T4">
  <name>Task 4: Remove debug console statement from useOrdersFeed.ts</name>
  <description>
Remove the console.error in src/hooks/useOrdersFeed.ts (line ~38):
```
console.error('Error loading orders in useOrdersFeed:', error);
```

Replace with nothing — the hook already returns error state to the consumer (FrontDesk.tsx) which handles display. The raw console.error is redundant noise.
  </description>
  <verification>
    - `grep "console\." src/hooks/useOrdersFeed.ts` returns nothing
  </verification>
</task>

<task type="execute" id="04-01-T5">
  <name>Task 5: Verify build passes</name>
  <description>
Run `npm run build` from the project root. The build must complete successfully with no TypeScript errors.

If there are errors, investigate and fix them before proceeding. Do not mark this task complete until the build is green.
  </description>
  <verification>
    - `npm run build` exits with code 0
    - No TypeScript errors in the build output
  </verification>
</task>

</tasks>
