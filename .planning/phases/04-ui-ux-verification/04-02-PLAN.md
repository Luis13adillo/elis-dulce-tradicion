---
phase: 04-ui-ux-verification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/phases/04-ui-ux-verification/04-VERIFICATION.md
autonomous: false

must_haves:
  truths:
    - "All customer-facing pages load without JavaScript errors"
    - "All staff-facing pages (Owner Dashboard, Front Desk) function correctly"
    - "Language toggle switches text between Spanish and English on all major pages"
    - "All modals open, display correct content, and close properly"
    - "No visual glitches or broken layouts on mobile, tablet, and desktop viewports"
  artifacts:
    - path: ".planning/phases/04-ui-ux-verification/04-VERIFICATION.md"
      provides: "Complete UI/UX verification results with pass/fail for UX-01 through UX-05"
      min_lines: 60
  key_links:
    - from: "Order wizard step validation"
      to: "Toast error feedback"
      via: "validateStep() in src/pages/Order.tsx"
      pattern: "validateStep"
    - from: "Language toggle"
      to: "All t() calls re-render"
      via: "useLanguage() in src/contexts/LanguageContext.tsx"
      pattern: "useLanguage"
---

<objective>
Verify that the entire application UI/UX meets the Phase 4 requirements across all pages, viewports, and interaction flows.

Purpose: Ensure the app is production-ready from a UI/UX perspective before launch. Catch any remaining visual glitches, broken interactions, or bilingual gaps.
Output: A verified, polished UI with documented results in 04-VERIFICATION.md.
</objective>

<execution_context>
@/Users/luismiguel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luismiguel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ui-ux-verification/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Start Dev Server</name>
  <action-required>
Start both servers if not already running:

```bash
npm run dev          # Vite dev server at http://localhost:5178
npm run server:dev   # Express backend at http://localhost:3001
```

Open http://localhost:5178 in your browser. Open DevTools (F12) and keep the Console tab visible throughout this verification.

Type "ready" when the servers are running and the home page is visible.
  </action-required>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Verify Customer-Facing Pages (UX-01)</name>
  <action-required>
Navigate to each URL and check: no crashes, no blank screens, no missing images/icons.

**Pages to test:**
1. http://localhost:5178/ — Home/Landing page
2. http://localhost:5178/order — Order wizard (all 5 steps visible)
3. http://localhost:5178/gallery — Photo gallery with lightbox
4. http://localhost:5178/menu — Product menu
5. http://localhost:5178/faq — FAQ accordion
6. http://localhost:5178/about — About page
7. http://localhost:5178/contact — Contact form
8. http://localhost:5178/order-tracking — Order tracker (enter any order ID)
9. http://localhost:5178/login — Login form
10. http://localhost:5178/order-confirmation — Post-payment screen (may show empty state)

**Check DevTools Console:** Any red JavaScript errors?

Report: which pages pass ✓ and which have issues ✗.
  </action-required>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Verify Staff-Facing Pages (UX-01 continued)</name>
  <action-required>
Log in as each role and verify their dashboards:

**Owner Dashboard:**
1. Log in as owner@elisbakery.com / ElisBakery123
2. Navigate to http://localhost:5178/owner-dashboard
3. Check: metrics display (Revenue Today, Orders Today, Average Ticket)
4. Check: all 6 tabs respond (Overview, Orders, Calendar, Products, Inventory, Reports)
5. Check: no console errors

**Front Desk:**
1. Log out, log in as orders@elisbakery.com / OrdersElisBakery123
2. Navigate to http://localhost:5178/front-desk
3. Check: order queue displays (or "No orders" message if empty)
4. Check: filter tabs work (All, New, Preparing, Pickup, Delivery, Done)
5. Check: no console errors

Report: pass ✓ or issues ✗ for each.
  </action-required>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Verify Responsive Design (UX-02)</name>
  <action-required>
Open DevTools → Toggle device toolbar (Ctrl+Shift+M / Cmd+Shift+M).

Test these pages at these widths:

**Mobile (375px):**
- / (home) — hero visible, text readable, no overflow
- /order — wizard steps fit on screen, buttons accessible
- /front-desk — order cards stack properly

**Tablet (768px):**
- / — layout shifts appropriately
- /owner-dashboard — sidebar collapses or adapts

**Desktop (1440px):**
- All pages — proper wide-screen layout, no stretched elements

Report: any pages that break at specific widths.
  </action-required>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Verify Bilingual Support (UX-03)</name>
  <action-required>
Find the language toggle (flag icon in the navbar or footer).

Test on these pages:
1. / (home) — switch to English, then back to Spanish. All section headings change?
2. /order — wizard labels switch language
3. /front-desk — order cards and buttons switch language
4. /owner-dashboard — tab labels and metrics switch language
5. /contact — form labels and submit button switch language

**Note:** Some product names (cake flavors, sizes) may stay in Spanish — that's acceptable.

Report: which pages have untranslated text that should be bilingual.
  </action-required>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Verify Modals and Interactions (UX-04)</name>
  <action-required>
Test each interactive element:

**Order Wizard validation (src/pages/Order.tsx):**
- Try advancing without selecting a date → should show validation error toast
- Try advancing without selecting a size → should show validation error toast
- Complete all 5 steps — does the wizard reach the payment page?

**Front Desk modals** (log in as Front Desk / orders@elisbakery.com):
- If any orders exist: click an order card → PrintPreviewModal opens
- PrintPreviewModal: toggle between Ticket and Invoice views
- PrintPreviewModal: click the X / back arrow to close
- If an order can be cancelled: open CancelOrderModal, verify it shows reason dropdown and close button

**FullScreenOrderAlert:**
- From the Owner Dashboard, place a test order through the wizard
- Switch to a second tab with /front-desk open
- Does the full-screen alert appear? Does the Dismiss button work?

Report: pass ✓ or issues ✗ for each modal.
  </action-required>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: Check Console for Errors (UX-05)</name>
  <action-required>
After all the above testing, review the DevTools Console:

1. How many red JavaScript errors appeared total?
2. Are there any recurring errors (same message 3+ times)?
3. Are there any network errors (404, 500) for critical resources?

Note: It's acceptable to have:
- Supabase auth session messages (informational)
- Service worker registration messages
- React dev mode warnings (not errors)

NOT acceptable:
- `TypeError: Cannot read properties of undefined`
- `Uncaught ReferenceError`
- Network 500 errors on API calls
- Missing chunk/asset 404 errors

Report: list any real errors found.
  </action-required>
</task>

<task type="execute" id="04-02-T8">
  <name>Task 8: Write Verification Document</name>
  <description>
Based on the human testing results from Tasks 2-7, create .planning/phases/04-ui-ux-verification/04-VERIFICATION.md.

Structure:
```markdown
# Phase 4: UI/UX Verification Results

**Verified:** [date]
**Environment:** Local dev (localhost:5178)

## UX-01: Pages Load Without Glitches
[Pass/Fail per page]

## UX-02: Responsive Design
[Pass/Fail per viewport]

## UX-03: Bilingual Support
[Pass/Fail per page, any gaps noted]

## UX-04: Interactive Elements
[Pass/Fail per modal/interaction]

## UX-05: Visual Consistency (No Console Errors)
[List of any real errors found, or "None"]

## Summary
[Overall pass/fail. Outstanding issues to track.]
```

If issues were found during testing:
- For minor issues (cosmetic, non-blocking): Document in the verification file under "Known Issues" and mark phase as PASSED WITH NOTES
- For critical issues (crashes, broken flows): Create follow-up tasks to fix before marking complete
  </description>
  <verification>
    - .planning/phases/04-ui-ux-verification/04-VERIFICATION.md exists with min 60 lines
    - Each UX requirement has a clear pass/fail result
  </verification>
</task>

</tasks>
