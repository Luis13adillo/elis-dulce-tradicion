---
phase: 08-menu-database-migration-and-price-security
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/payments.js
autonomous: true
requirements:
  - SEC-03

must_haves:
  truths:
    - "No Square imports, client initialization, or API calls remain in backend/routes/payments.js"
    - "The npm run build passes without import errors from deleted Square files"
    - "All Square-specific routes are removed (create-payment, /square/:paymentId, refund route calling Square, create-checkout)"
    - "Stripe-only routes and helper functions remain intact and unmodified"
    - "square.ts and SquarePaymentForm.tsx are deleted from disk"
  artifacts:
    - path: "backend/routes/payments.js"
      provides: "Stripe-only payment routes (verify, order lookup), no Square references"
      contains: "export default router"
  key_links:
    - from: "backend/routes/payments.js"
      to: "square npm package"
      via: "import statement"
      pattern: "SquareClient|SquareEnvironment|squareClient|paymentsApi|refundsApi"
---

<objective>
Remove all Square dead code from the codebase. Square was the original payment provider but has been fully replaced by Stripe via Supabase Edge Functions. No Square routes are called anywhere in the live app.

Purpose: Dead code creates maintenance burden, confusing imports of an unused SDK, and a false impression that Square is active. Removing it clarifies that Stripe is the sole payment provider.
Output: backend/routes/payments.js cleaned to Stripe-only; src/lib/square.ts deleted; src/components/payment/SquarePaymentForm.tsx deleted.
</objective>

<execution_context>
@/Users/luismiguel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luismiguel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-menu-database-migration-and-price-security/08-CONTEXT.md

<interfaces>
<!-- Key structure of backend/routes/payments.js (568 lines) -->
<!-- Square sections to REMOVE (lines 1-21, 36-231, 237-312, 315-334, 360-482, 532-566): -->
<!--   - Lines 1-21: Square imports + SquareClient init + SQUARE_ACCESS_TOKEN + SQUARE_LOCATION_ID -->
<!--   - Lines 36-231: POST /create-payment (Square payment creation + order INSERT) -->
<!--   - Lines 237-312: POST /verify (Square payment verification via paymentsApi.getPayment) -->
<!--   - Lines 315-334: GET /square/:paymentId (Square payment lookup) -->
<!--   - Lines 360-482: POST /:paymentId/refund (Square refund flow) -->
<!--   - Lines 487-530: logFailedPayment() helper (calls Square paymentsApi) — REMOVE -->
<!--   - Lines 532-566: POST /create-checkout (legacy stub returning null data) — REMOVE -->

<!-- Stripe-only sections to KEEP (do NOT touch): -->
<!--   - Lines 340-358: GET /order/:orderId — DB-only payment lookup, no Square -->

<!-- generateOrderNumber() function at lines 26-30 — REMOVE (only used by Square create-payment) -->

<!-- After cleanup, the file should contain: -->
<!--   import express from 'express'; -->
<!--   import pool from '../db/connection.js'; -->
<!--   import { requireAuth } from '../middleware/auth.js'; -->
<!--   const router = express.Router(); -->
<!--   GET /order/:orderId — kept intact -->
<!--   export default router; -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Square source files</name>
  <files>src/lib/square.ts, src/components/payment/SquarePaymentForm.tsx</files>
  <action>
Delete both files from disk:
- `src/lib/square.ts` (81 lines — Square SDK wrapper, not imported anywhere in the codebase)
- `src/components/payment/SquarePaymentForm.tsx` (170 lines — Square payment form component, not imported anywhere)

These files are confirmed dead code via codebase search — no other files import from either path. Per user decision in 08-CONTEXT.md: "Remove exactly these files/sections."

Use the Bash tool: `rm /Users/luismiguel/Desktop/elis-dulce-tradicion/src/lib/square.ts && rm /Users/luismiguel/Desktop/elis-dulce-tradicion/src/components/payment/SquarePaymentForm.tsx`

After deletion, run `npm run build` from the project root to confirm no import errors.
  </action>
  <verify>
    <automated>cd /Users/luismiguel/Desktop/elis-dulce-tradicion && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Both files no longer exist on disk. Build completes without errors referencing square.ts or SquarePaymentForm.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Square code from backend/routes/payments.js</name>
  <files>backend/routes/payments.js</files>
  <action>
Rewrite backend/routes/payments.js keeping ONLY the Stripe-compatible sections. The final file must contain:

1. **Imports** — keep only `express`, `pool` (from `../db/connection.js`), and `requireAuth` (from `../middleware/auth.js`). Remove `SquareClient`, `SquareEnvironment` import from 'square'.

2. **Remove entirely:**
   - `squareClient` initialization block (lines 9-21)
   - `SQUARE_LOCATION_ID` and `SQUARE_ACCESS_TOKEN` constants
   - `generateOrderNumber()` function (only used in Square create-payment)
   - `POST /create-payment` route (Square payment + order creation, lines 36-231)
   - `POST /verify` route (Square payment verification via paymentsApi.getPayment, lines 237-312)
   - `GET /square/:paymentId` route (lines 315-334)
   - `POST /:paymentId/refund` route (Square refund flow, lines 360-482)
   - `logFailedPayment()` helper function (lines 487-530)
   - `POST /create-checkout` legacy stub (lines 532-566)

3. **Keep intact (do NOT modify):**
   - `GET /order/:orderId` route (lines 340-358) — DB-only payment lookup, no Square references

4. **Result:** A short, clean file (~25 lines) containing: imports, router init, GET /order/:orderId, export default router.

Per user decision in 08-CONTEXT.md: "No replacement Stripe management endpoints needed — Stripe payments already handled via Supabase Edge Functions."

Do NOT add any new Stripe routes or code. Only remove Square code.
  </action>
  <verify>
    <automated>cd /Users/luismiguel/Desktop/elis-dulce-tradicion && grep -c "square\|Square\|squareClient" backend/routes/payments.js && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>grep for "square|Square|squareClient" in payments.js returns 0 matches. npm run build passes. GET /order/:orderId route still present in the file.</done>
</task>

</tasks>

<verification>
- `src/lib/square.ts` does not exist: `ls src/lib/square.ts` returns error
- `src/components/payment/SquarePaymentForm.tsx` does not exist: `ls src/components/payment/SquarePaymentForm.tsx` returns error
- `grep -r "from.*square\|require.*square" src/ backend/` returns no matches
- `npm run build` exits 0
- `grep -c "SquareClient\|squareClient\|SquareEnvironment" backend/routes/payments.js` returns 0
- `grep "order/:orderId" backend/routes/payments.js` returns the GET route (kept intact)
</verification>

<success_criteria>
All Square dead code removed. Build green. No Square SDK imports remain in src/ or backend/. The sole active payment provider is Stripe via Supabase Edge Functions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-menu-database-migration-and-price-security/08-01-SUMMARY.md`
</output>
